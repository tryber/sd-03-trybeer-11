const productsControllers = require('./products');
const usersControllers = require('./users');
const salesControllers = require('./sales');

module.exports = {
  productsControllers,
  usersControllers,
  salesControllers,
};
